{
  "private": true,
  "name": "{{ name }}",
  "description": "Preact template using typescript, scss and Bulma CSS framework.",
  "author": {
    "name": "Spelmakare Jens Nilsson AB",
    "url": "https://spelmakare.se"
  },
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact serve",
    "bs": "preact build && preact serve",
    "dev": "preact watch"
  },
  "devDependencies": {
    "if-env": "^1.0.0",
    "node-sass": "^4.9.4",
    "preact-cli": "^2.1.0",
    "preact-cli-plugin-typescript": "^0.2.2",
    "sass-loader": "^7.1.0",
    "typings-for-css-modules-loader": "^1.7.0"
  },
  "dependencies": {
    "bulma": "^0.7.2",
    "preact": "^8.2.6",
    "preact-async-route": "^2.2.1",
    "preact-helmet": "^4.0.0-alpha-3",
    "preact-router": "^2.5.7"
  }
}